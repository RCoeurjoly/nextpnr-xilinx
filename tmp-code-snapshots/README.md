# Temporary code snapshots preserved from /tmp

This directory preserves code-like nextpnr/OpenXC7 source artifacts that were
previously only under `/tmp`. Logs, build products, chipdb binaries, bitstreams,
frames, and routed outputs were not copied here.

Contents:

- `openxc7-nextpnr-patched/`: selected source files from `/tmp/openxc7-nextpnr-patched` that may contain PHASER/OpenXC7 support deltas.

This is a preservation snapshot, not the active implementation path. Diff these
files against the working tree before promoting any change.
